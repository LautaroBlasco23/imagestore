services:
  imagestore:
    build: .
    ports:
      - "50051:50051"
      - "8087:8087"
    volumes:
      - ./images:/root/images
      - imagestore_data:/root
    restart: unless-stopped
    environment:
      - TZ=UTC

volumes:
  imagestore_data:
